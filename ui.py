from tkinter import *
from tkinter import ttk

# Create the root window
root = Tk()

# Create a frame within the root window with padding
frm = ttk.Frame(root, padding=10)

# Pack the frame within the root window
frm.pack()

# Start the Tkinter event loop
root.mainloop()
